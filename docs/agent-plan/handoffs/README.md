# Accepted task handoffs

Each completed part writes `NN.md` here from `../HANDOFF_TEMPLATE.md`. These
files are the minimal context passed to fresh agents. There are no accepted
implementation handoffs yet.
