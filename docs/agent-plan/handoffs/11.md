# Part 11 handoff — local integration repair and release evidence

State: blocked (final visual acceptance/readiness evidence)
Agent or work session: Codex repair pass
Date: 2026-09-25
Base revision: working tree at start of repair pass; no commit created

## Result

All eight existing examples compile locally. The three previously failing
examples now render: customization has a straight downward chain; structures has
contained notes, measured captions and connected boundary routes; complex opts
into two complete pages rather than clipping an oversized single picture.
Continuation labels no longer expose internal flow IDs. The public workflow is
ordinary LuaLaTeX with colocated package files and no shell escape or preprocessing.

A full 20-case source catalogue, development trace capture and independent
geometry checker now exist. Every case is compiled twice and its traces compared.
The checker verifies schema, node/flow coverage, endpoint attachment, orthogonality,
reversals, text/block/path clearance, structure containment and declared ports,
hard rows/columns/notes, page bounds when multipage is enabled and unchanged scale.
Compilation success and geometry validity are not human visual readiness ratings.

Final human visual sign-off is reserved to the user. The user explicitly deferred
Overleaf testing to their own final test; it was not performed here. Parts 09 and
11 remain blocked because final visual readiness is not yet established.

## Changed files

Part 09 lists the layout modules and regression fixtures changed in this pass.
Additional integration changes:

- `tikzffbd.sty`: warn explicitly on retired prototype options instead of silently
  accepting ineffective settings. Active `block-width` still controls measurement.
- `tikzffbd-bridge.lua`: log router calls/work and print suggested warning actions.
- `tikzffbd-renderer.lua`, `tikzffbd-renderer.code.tex`: render measured structure
  captions and split-group styles; correct caption Y anchors; preserve measured
  natural label width instead of rewrapping labels into a smaller box.
- `examples/complex.tex`: explicit multipage opt-in. `structures.tex` and
  `1st-real-world-use.tex`: remove retired options. The tracked real-world PDF and
  SyncTeX output also regenerated during the session.
- `tests/contract_schema.py`, `tests/run_contracts.py`: accept both user nodes and
  generated connectors in node metrics, and validate the expanded source catalogue.
- `tests/fixtures/benchmark-report.json`, `release-report.json`, `references/`,
  `REVIEW.md`: saved timing, geometry, PDF and review evidence.
- `README.md`, `.gitignore`, `CONTRACTS.md`, `STATUS.md`, handoffs 09/11:
  installation/options, artifact handling and acceptance documentation.

## Acceptance evidence

On LuaHBTeX 1.17.0, TeX Live 2023/Debian:

- All phase suites passed: model 91, constraints 27, analysis 39, planner 158,
  placement 116, routing 218, solver 234, renderer 55 assertions.
- `python3 tests/run_contracts.py`: 9 tests passed.
- `python3 tests/run_integration.py`: 20/20 automated cases passed across two
  compilations each. Nineteen sources produced PDFs; `columns-hard-conflict`
  correctly diagnosed a block wider than its frame. Its original proposed
  max-columns/endrow-only conflict was not an actual contradiction, so the fixture
  now explicitly includes measured-width infeasibility and names the hard controls.
- `tests/bridge_measurement.tex` passed both environment markers;
  `tests/renderer_scene.tex` passed `RENDERER-SCALE-OK`, with no shell escape.
- `examples/large.tex`, `large-multipage.tex`, `scaled.tex` compiled separately;
  the other five examples are included in the catalogue run.
- A directory containing only `main.tex`, `tikzffbd.sty`, colocated Lua modules and
  the three `.code.tex` files compiled the complex two-page document successfully
  with `lualatex -no-shell-escape`. This is local portability evidence, not an
  Overleaf result. The prepared archive is `build/overleaf-project.zip`.

First and repeat compilations (OS caches were not flushed), peak RSS in KiB:

| Blocks | First seconds / KiB | Repeat seconds / KiB | Candidates | Total router work | Pages |
| --- | --- | --- | --- | --- | --- |
| 10 | 1.569 / 158420 | 1.569 / 158440 | 8/64 | 156 | 1 |
| 30 | 2.292 / 166800 | 2.338 / 166660 | 8/64 | 486 | 1 |
| 50 | 3.172 / 173080 | 3.225 / 163448 | 8/64 | 808 | 1 |
| 64 | 3.791 / 171008 | 3.817 / 171308 | 8/64 | 1072 | 2 |

None of these benchmark searches exhausted its budget. Catalogue compile times
were about 1.2–4.6 seconds per run. Dense annotation searches can reach the
64-attempt cap and return the best valid Scene with an explicit warning. The
64-block single-page adversary retains scale 1 and warns about overflow; its
clipped PDF is intentionally not rated publication-ready.

Visual references and human-review prompts are in `tests/fixtures/REVIEW.md`.
Agent notes are separate from the still-null human ratings. Readiness proportions
for <10, 10–30 and 30–50 blocks remain unmeasured; no target percentage is claimed.
The session stopped at the user's request to wrap up, after saving these results.

## Contract changes

No breaking changes. Measured structure captions, group ownership and router-work
metadata are additive version 1 fields documented in `CONTRACTS.md`.

## Known limits and risks

Dense branch routing can still contain confusing shared corridors and long
turns: inspect `branches-30-unequal` and `joins-feedback-42`. The structure example
has a condition label touching its group border. These are visual concerns even
though hard geometry checks pass. Full visual review/calibration and readiness
measurements remain open. Overleaf testing is explicitly deferred to the user.

Version 0.5.0 remains an integration preview. Do not advertise a stable release
until visual acceptance and readiness evidence are resolved.

## Next agent needs

Start with `tests/fixtures/REVIEW.md`, saved reports and the user's visual ratings.
Use `python3 tests/run_integration.py <fixture-id>` for focused reproductions;
ordinary package users need no scripts. `build/visual-review.zip` collects the
PDFs/checklist, and `build/overleaf-project.zip` is ready for the user's upload.
Retain blocked status until the outstanding visual gate is actually met.
